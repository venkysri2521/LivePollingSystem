# Copy to .env and fill in. Never commit the real .env.
PORT=8080
APP_ENV=development

# MongoDB Atlas or local: mongodb://localhost:27017
MONGO_URI=mongodb+srv://Venkatramanan_S:sri2307@cluster0.52pan.mongodb.net/?appName=Cluster0
MONGO_DB=livepolls

# Upstash / Redis Cloud or local: redis://localhost:6379
REDIS_URL=redis://localhost:6379

# 32+ random chars. Generate: openssl rand -base64 48
JWT_SECRET=change-me-to-a-long-random-string

# Comma separated. The deployed frontend origin goes here.
CORS_ORIGINS=http://localhost:5173

# Used to build the shareable poll URL returned to the client
PUBLIC_APP_URL=http://localhost:5173
